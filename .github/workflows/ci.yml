name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install frontend dependencies
        run: npm ci
        working-directory: frontend
      - name: Test frontend
        run: npm test
        working-directory: frontend
      - name: Build frontend
        run: npm run build
        working-directory: frontend
      - name: Audit frontend dependencies
        run: npm audit --audit-level=high
        working-directory: frontend

  container:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux/amd64
            arch: amd64
          - platform: linux/arm64
            arch: arm64
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: docker/setup-qemu-action@96fe6ef7f33517b61c61be40b68a1882f3264fb8 # v4.2.0
      - uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0
      - name: Build ${{ matrix.platform }} image
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: .
          platforms: ${{ matrix.platform }}
          load: true
          tags: qscn:ci-${{ matrix.arch }}
          build-args: |
            QSCN_BUILD_REVISION=${{ github.sha }}
            QSCN_BUILD_TIMESTAMP=ci
          cache-from: type=gha,scope=qscn-${{ matrix.arch }}
          cache-to: type=gha,mode=max,scope=qscn-${{ matrix.arch }}
      - name: Run backend and packaging tests
        run: >-
          docker run --rm --platform ${{ matrix.platform }}
          -v "$GITHUB_WORKSPACE:/workspace:ro" -w /workspace
          -e PYTHONPATH=/workspace/backend qscn:ci-${{ matrix.arch }}
          python -m unittest discover -s tests -v
      - name: Verify bundled tools and database indexes
        run: >-
          docker run --rm --platform ${{ matrix.platform }} qscn:ci-${{ matrix.arch }}
          sh -c 'hmmscan -h >/dev/null && prodigal -v >/dev/null 2>&1 &&
          test -f /app/database-runtime/QSPdatabase.hmm.h3p &&
          test -f /app/database-runtime/kegg_m02024.hmm.h3p'

  windows-launcher:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Parse PowerShell launcher
        shell: powershell
        run: |
          $tokens = $null
          $parseErrors = $null
          [System.Management.Automation.Language.Parser]::ParseFile(
            (Resolve-Path 'packaging/windows/qscn.ps1'),
            [ref]$tokens,
            [ref]$parseErrors
          ) | Out-Null
          if ($parseErrors.Count) { $parseErrors | Format-List | Out-String | Write-Error }
      - name: Test PowerShell launcher behavior
        shell: powershell
        run: tests/windows_launcher.ps1

  shell-launchers:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            script: packaging/linux/qscn.sh
            wrapper: packaging/linux/qscn.sh
          - os: macos-latest
            script: packaging/macos/qscn.sh
            wrapper: packaging/macos/QSCN.command
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Parse and inspect launcher
        shell: bash
        run: |
          sh -n "${{ matrix.script }}"
          sh -n "${{ matrix.wrapper }}"
          test -x "${{ matrix.script }}"
          test -x "${{ matrix.wrapper }}"

  release-packages:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Build three platform packages
        env:
          QSCN_RELEASE_OUTPUT_DIR: ${{ runner.temp }}/qscn-releases
        run: >-
          ./scripts/package_release.sh 0.4.0
          ghcr.io/emmettpeng/qscn@sha256:aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
      - name: Verify checksums and package contents
        env:
          RELEASE_DIR: ${{ runner.temp }}/qscn-releases
        shell: bash
        run: |
          cd "$RELEASE_DIR"
          sha256sum --check SHA256SUMS
          unzip -q QSCN-v0.4.0-windows.zip -d windows
          unzip -q QSCN-v0.4.0-macos.zip -d macos
          tar -xzf QSCN-v0.4.0-linux.tar.gz -C .
          test -f windows/QSCN-v0.4.0-windows/windows/QSCN.cmd
          test -f macos/QSCN-v0.4.0-macos/macos/QSCN.command
          test -x QSCN-v0.4.0-linux/linux/QSCN.sh
          test -f QSCN-v0.4.0-linux/.env.example
          grep -q 'ghcr.io/emmettpeng/qscn@sha256:aaaa' QSCN-v0.4.0-linux/compose.yml
          ! grep -q '__QSCN_IMAGE__' QSCN-v0.4.0-linux/compose.yml
          ! grep -R -q 'down -v' windows macos QSCN-v0.4.0-linux
